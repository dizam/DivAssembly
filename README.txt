Assembly program to divide two chosen numbers and find the quotient and the remainder.
Binary long-division was used here to avoid using the DIV and MUL instructions.
The resulting quotient is stored in the EAX register, and the remainder is stored in the EDX register.

To use the provided tester, simply run the tester.py script with python using the command: python3 tester.py.

